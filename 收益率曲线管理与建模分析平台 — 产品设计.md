# 收益率曲线管理与建模分析平台 — 产品设计

> 基于《收益率曲线管理与建模分析平台 — 产品功能框架》形成的产品设计方案
> 版本：V1.0   日期：2026-08-17
> 关系：本设计与 ALMD（资本/流动性）、IALMD（保险偿付能力）、ALMT（资产负债计划模拟）共同构成银行经营智能分析平台矩阵

---

## 0. 文档说明

本文档从需求框架出发，落到**产品定位、数据架构、模块设计、关键流程、接口契约、智能体编排、非功能设计、实施路径与风险**九个维度，给出可落地实施的产品设计。

---

## 1. 产品定位与价值主张

### 1.1 一句话定位

> **统一曲线数据底座 + 可配置曲线构建引擎 + 智能分析决策助手** —— 让曲线"采得到、建得准、看得清、算得动、调得动"。

### 1.2 核心价值主张（对应需求四大痛点）

| 痛点 | 平台能力 | 价值量化指标 |
|---|---|---|
| 数据来源分散、采集维护成本高 | L1 多源适配器 + 统一采集调度 | 减少 80% 手工搬运，1 人可维护全行曲线数据 |
| 曲线构建黑盒、不可配置 | L3 引擎插件化 + 全链路留痕 | 任一期限点可回溯至原始采集+调整参数 |
| 分析能力薄弱、难以支撑计量 | L4 敏感度/情景/压力/预测四件套 | NII/EVE 计算耗时从天级降为分钟级 |
| 版本管理缺失、监管审计难 | L2 版本对比 + 血缘追踪 + 操作审计 | 满足 IRRBB 报送与监管现场检查需要 |

### 1.3 在银行经营智能分析矩阵中的定位

```
┌──────────────────────────────────────────────────────────────────────────┐
│                       银行经营智能分析平台矩阵                              │
├───────────────┬────────────────┬───────────────────┬────────────────────┤
│  ALMD         │   IALMD        │   ALMT            │   CURV（本平台）    │
│ 资本/流动性    │   偿付能力      │   计划模拟         │   曲线引擎         │
├───────────────┼────────────────┼───────────────────┼────────────────────┤
│ 监管合规       │   行业监管      │   经营决策         │   定价基准          │
│ 47行 59指标    │   201保司 20指标 │   4引擎            │   多源 13+种曲线   │
│ 报告→指标→分析 │   报告→指标→分析 │   假设→结果→敏感性 │   采集→构建→分析   │
└───────────────┴────────────────┴───────────────────┴────────────────────┘
                ↑                        ↑
        共享：LLM / Agent / 本体 / 部署 / SSO
                ↓
        CURV 提供：FTP基准曲线 / 估值折现曲线 / 风险计量利率冲击源
```

CURV 是矩阵的**「定价底层」**，为 ALMD/ALMT 的利率风险计量与计划模拟提供基准曲线服务。

---

## 2. 目标用户与场景

### 2.1 用户角色矩阵（沿用需求 1.3，扩展典型场景）

| 用户角色 | 典型岗位 | 核心场景 | 平台接触点 |
|---|---|---|---|
| 资负管理岗 | 资负管理处 | 利率走势监控、月度 IRRBB 计量 | L4 走势+敏感度、L5 监管报表 |
| FTP 定价岗 | 财务/资负 | FTP 基准曲线构建、存贷款加点 | L3 构建引擎、L5 FTP 应用 |
| 估值核算岗 | 会计/资金 | 债券估值、衍生品定价 | L2 折现曲线查询 API |
| 风险管理岗 | 风控 | 压力测试、情景模拟、监管报送 | L4 情景+压力、L5 监管 |
| 数据治理岗 | 数据中心 | 数据源接入、采集调度、数据质量 | L1 采集、L2 数据治理 |
| 系统管理员 | 科技 | 用户/权限/任务/告警配置 | L1 监控、L2 权限 |

### 2.2 典型业务场景（业务故事）

1. **"我要看 10 年期国债近 5 年的走势"** → L4 单曲线时序分析
2. **"帮我构建一条无风险收益率曲线"** → L3 拼接+拟合（Repo+国债）
3. **"如果利率上行 100bp，我的 EVE 变化多少？"** → L4 情景模拟+敏感度
5. **"信用利差什么时候收窄到历史均值？"** → L4 形态指标+回测
6. **"今天的曲线数据有什么异常？"** → L1 监控 + L2 校验告警
7. **"FTP 基准曲线需要更新到最新交易日"** → L3 + L5 一键发布
8. **"曲线数据给 ALMT 做计划模拟"** → L5 标准 API 输出

---

## 3. 整体产品架构

### 3.1 五层 + 三横架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       L5 业务应用层（场景化交付）                              │
│   FTP定价    估值核算    ALM分析    流动性风险    资产配置    监管报送         │
├─────────────────────────────────────────────────────────────────────────────┤
│                       L4 分析建模层（智能计算）                                │
│   走势分析    形态指标    利差分析    关键利率久期    情景模拟    压力测试      │
│   预测建模（三期）  回溯测试   智能对话(LLM Agent)                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                       L3 曲线构建引擎层（数值计算核心）                        │
│   拼接    插值    平滑    拟合(NS/NSS/Polynomial/Spline)    关键期限点提取      │
├─────────────────────────────────────────────────────────────────────────────┤
│                       L2 数据管理层（治理与版本）                              │
│   曲线定义    利率数据存储    派生曲线    版本管理    数据血缘    校验规则       │
├─────────────────────────────────────────────────────────────────────────────┤
│                       L1 数据采集层（多源适配）                                │
│   数据源适配    采集调度    数据清洗    采集监控    异常告警                     │
└─────────────────────────────────────────────────────────────────────────────┘
        ↑                                              ↑
   横向：安全/权限       横向：监控运维        横向：智能体编排(LangGraph)
   横向：审计日志         横向：配置中心         横向：本体知识
```

### 3.2 与现有平台的技术栈复用

| 维度 | 复用 ALMD/IALMD/ALMT 的能力 | CURV 自身新增 |
|---|---|---|
| 后端框架 | FastAPI + Uvicorn | — |
| 前端 | React + AntD + Vite | CURV 专用可视化组件（曲线图/曲面图/热力图） |
| 数据库 | MySQL + Redis | 时序数据库（InfluxDB/TimescaleDB，可选） |
| 智能体 | LangGraph + DeepSeek LLM | CURV 专用 Agent：CURVE_CHAT |
| 部署 | systemd + nginx + 子路径 | `/curv/` 子路径独立部署 |
| 本体/版本/血缘 | 设计模式复用 | CURV 领域本体（曲线/利率/期限/拟合模型） |

### 3.3 服务拓扑

```
                    ┌──────────────┐
                    │  统一门户     │ ← http://43.143.253.186/
                    │  /almd /ialmd │
                    │  /almt /curv  │
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼─────┐      ┌─────▼────┐      ┌──────▼──────┐
   │ curv-    │      │ curv-    │      │ curv-       │
   │ frontend │ ←→   │ backend  │ ←→  │ LLM/Agent   │
   │ /var/www │      │ :8003    │      │ DeepSeek    │
   │ /curv/   │      │ /opt/    │      │             │
   └──────────┘      │ curv/    │      └─────────────┘
                     └────┬─────┘
                          │
                  ┌───────┼───────┐
                  │       │       │
            ┌─────▼─┐ ┌───▼───┐ ┌──▼──────┐
            │MySQL  │ │Redis  │ │TSDB(可选)│
            │curv_db│ │cache  │ │rate_data │
            └───────┘ └───────┘ └─────────┘
                          │
                  ┌───────▼──────┐
                  │ 外部数据源    │
                  │ 中债/外汇/Wind│
                  └──────────────┘
```

---

## 4. 数据架构

### 4.1 关键实体（ER 总览）

```
数据源 ──┬── 采集任务 ── 采集记录
         └── 曲线定义(1:n) ──┬── 利率数据(曲线×日期×期限)
                             ├── 曲线版本(曲线×日期×版本)
                             ├── 派生公式 → 派生曲线(曲线定义)
                             ├── 拟合参数(曲线×日期×版本×模型)
                             ├── 形态指标(曲线×日期)
                             ├── 情景定义 ── 情景冲击结果
                             └── 血缘链路(任一数据点 → 全链路)
```

### 4.2 核心表设计（精简版）

> 表前缀 `curv_`；公共字段 id/status/is_deleted/created_by/updated_by/created_at/updated_at；JSON 字段 `_json` 后缀；utf8mb4

| 表名 | 关键字段 | 说明 |
|---|---|---|
| `curv_data_source` | code, name, type(API/FILE/MANUAL), config_json, auth_json, status | 数据源配置 |
| `curv_collection_task` | source_id, schedule_cron, params_json, retry_policy_json | 采集任务 |
| `curv_collection_log` | task_id, run_time, status, record_count, error_msg, duration_ms | 采集日志 |
| `curv_curve_definition` | code, name, currency, rate_type, day_count, tenor_set_json, source_mapping_json | 曲线定义 |
| `curv_rate_data` | curve_code, trade_date, tenor, rate_value, source_version, collection_id, status | 利率数据（核心表，按曲线×日期×期限三维） |
| `curv_curve_version` | curve_code, trade_date, version_no, version_status(raw/adjusted/official), parent_version_id | 曲线版本 |
| `curv_derived_curve` | curve_code, base_curve_codes_json, formula, auto_update | 派生曲线定义 |
| `curv_fitting_param` | curve_code, trade_date, version_no, model_type(NS/NSS/...), params_json, rmse, r2 | 拟合参数 |
| `curv_shape_metric` | curve_code, trade_date, metric_type(长短期利差/斜率/曲率/倒挂区间), value, params_json | 形态指标 |
| `curv_scenario` | code, name, type(parallel/steepener/.../), shock_json, description | 情景定义 |
| `curv_scenario_result` | scenario_id, curve_code, trade_date, pv_change, nii_change, eve_change, asset_liability_id | 情景结果 |
| `curv_lineage` | data_id, upstream_sources_json, operations_json, reason | 血缘链路 |
| `curv_validation_rule` | curve_code, rule_type, threshold, enabled | 校验规则 |
| `curv_plugin_model` | code, name, type(smooth/fit/...), impl_path, params_schema, enabled | 拟合/平滑算法插件 |
| `curv_smart_dialogue` | session_id, user_id, query, agent_trace_json, result_json, refs_json | 智能问数对话 |

辅助系统表：`sys_user / sys_role / sys_permission / sys_llm_config / sys_audit_log`（沿用 ALMD 模式）

### 4.3 关键三维模型：利率数据

`curv_rate_data` 是平台的事实表，所有分析都基于它。

- 主键：(curve_code, trade_date, tenor, source_version)
- 索引：
  - `(curve_code, trade_date)` — 时序查询
  - `(trade_date)` — 全市场快照
  - `(curve_code, tenor, trade_date)` — 单期限历史
- 存储策略：
  - **一期**：MySQL 单表 + 复合索引（足够支撑 13 条曲线 × 30 期限点 × 5 年 = ~7000 行/天）
  - **二期/三期**：升级至 TimescaleDB hypertable，按月分区，亿级数据秒级响应

### 4.4 血缘追踪模型

```
[原始采集] curv_data_source → curv_collection_log → curv_rate_data(source_version=raw)
       ↓ 清洗/转换
[清洗入库] curv_rate_data(source_version=cleaned)
       ↓ 派生计算
[派生曲线] curv_derived_curve → curv_rate_data(source_version=derived)
       ↓ 人工调整
[调整版] curv_rate_data(source_version=adjusted)
       ↓ 业务确认
[正式版] curv_rate_data(source_version=official) ← curv_curve_version(version_status=official)
```

每个 `rate_data` 行可向上回溯至：原始数据源 → 采集任务 → 清洗规则 → 派生公式 → 调整操作 → 当前版本。

---

## 5. 模块详细设计

### 5.1 L1 数据采集层

#### 5.1.1 数据源适配器（Adapter Pattern）

```python
# 设计模式：策略模式 + 工厂模式
class DataSourceAdapter(ABC):
    @abstractmethod
    def fetch(self, params: dict) -> List[RawRateRecord]: ...
    @abstractmethod
    def parse(self, raw) -> List[NormalizedRateRecord]: ...

class ChinaBondAdapter(DataSourceAdapter): ...   # 中债登
class ShiborAdapter(DataSourceAdapter): ...       # 外汇交易中心
class WindAdapter(DataSourceAdapter): ...         # Wind
class FileImportAdapter(DataSourceAdapter): ...    # Excel/XML/CSV
class ManualAdapter(DataSourceAdapter): ...       # 手工录入

# 注册表
ADAPTER_REGISTRY = {
    'china_bond': ChinaBondAdapter,
    'shibor': ShiborAdapter,
    'wind': WindAdapter,
    'file': FileImportAdapter,
    'manual': ManualAdapter,
}
```

新增数据源 = 新增一个 `DataSourceAdapter` 实现类 + 在注册表登记一行。

#### 5.1.2 采集调度

- 基于 APScheduler / Celery Beat
- 三种调度策略：
  - 日终批量（T+1 06:00 全量拉取）
  - 盘中增量（每 30 分钟拉取活跃期限）
  - 实时事件（数据源推送 Webhook）
- 重试策略：3 次指数退避，失败转人工告警

#### 5.1.3 数据清洗流水线

```
原始数据 → 标准化(normalize) → 校验(validate) → 转换(transform) → 入库(load)
                                  ↓ 异常
                              标记 + 告警 + 待复核队列
```

清洗规则包括：
- 字段映射（中债英文字段 → 内部 schema）
- 缺失值处理（前值/插值/标记）
- 异常值检测（基于历史波动率 + 相邻期限检测）
- 计息口径转换（ACT/365、30/360、单利/复利）
- 币种/单位统一

### 5.2 L2 数据管理层

#### 5.2.1 曲线定义配置（可视化表单）

- 基本属性：编码、名称、币种、所属分类
- 利率类型：即期/远期/到期收益率/平价收益率
- 计息规则：单利/复利/连续复利、计息基准
- 期限结构：标准期限点 + 自定义期限
- 数据源：关联 curv_data_source + 字段映射

#### 5.2.2 版本与血缘（同上 4.4）

每条曲线每日保留三个版本：**raw → cleaned → official**，支持版本对比和回滚。

#### 5.2.3 校验规则引擎

- 基础校验：非空、范围(0-20%)、单调性
- 业务校验：日终对账（与官方发布值对比，差异 > 阈值告警）
- 规则可按曲线配置

### 5.3 L3 曲线构建引擎（核心）

#### 5.3.1 设计原则

- **可配置**：所有构建操作通过参数描述，不写死
- **可追溯**：每次构建结果都记录"输入参数 + 算法 + 中间结果 + 输出"
- **可复现**：相同输入必得相同输出
- **可扩展**：新增算法以插件方式接入

#### 5.3.2 引擎架构

```
构建请求 → 曲线定义解析 → 参数校验 → 算子编排 → 执行 → 结果归档
                                                    ↓
                                          curv_curve_version(version_no=build)
                                          curv_rate_data(source_version=build_xxx)
                                          curv_fitting_param(...)
                                          curv_lineage(...)
```

#### 5.3.3 五大算子（详细设计）

##### (1) 拼接 (Curve Splicing)

```python
def splice(short_curve, long_curve, splice_tenor=1.0, 
           overlap_range=(0.5, 2.0), mode='linear_transition'):
    """
    mode: 'short_priority' | 'long_priority' | 'mean' | 'linear_transition'
    """
    # 1. 切分短端/长端
    short_part = short_curve[short_curve.tenor <= splice_tenor]
    long_part = long_curve[long_curve.tenor >= splice_tenor]
    
    # 2. 重叠区间按权重过渡
    overlap = overlap_region(short_part, long_part, overlap_range)
    blended = blend_by_distance(overlap, mode)
    
    # 3. 拼接并校验连续性 + 平滑性
    result = concat(short_part, blended, long_part)
    validate_continuity(result, splice_tenor)
    return result
```

验收：拼接点连续 + 一阶导数平滑（线性过渡模式） + 各点可追溯至源曲线。

##### (2) 插值 (Interpolation)

| 方法 | 实现 | 适用 |
|---|---|---|
| 线性插值 | numpy.interp | 简单估算 |
| 对数线性 | log(DF)线性插值 | 贴现因子 |
| 三次样条 | scipy.interpolate.CubicSpline | 光滑 |
| PCHIP | scipy.interpolate.PchipInterpolator | 保持单调 |
| Nelson-Siegel 插值 | NSS 模型预测 | 经济含义明确 |

接口：`interpolate(curve, target_tenors, method='pchip') -> RateSeries`

##### (3) 平滑 (Smoothing)

- 移动平均：`pandas.rolling(window=5).mean()`
- EWMA：`pandas.ewm(halflife=3).mean()`
- LOESS：`statsmodels.nonparametric.smoothers_lowess.lowess`

输出：平滑后曲线 + 残差序列（用于可视化对比）

##### (4) 拟合 (Fitting)

```python
class FittingModel(ABC):
    @abstractmethod
    def fit(self, tenors: np.ndarray, rates: np.ndarray) -> FitResult: ...
    @abstractmethod
    def predict(self, tenors: np.ndarray) -> np.ndarray: ...

class NelsonSiegel(FittingModel):
    """β0 + β1*((1-exp(-t/τ))/(t/τ)) + β2*((1-exp(-t/τ))/(t/τ) - exp(-t/τ))"""
    ...

class Svensson(FittingModel):
    """NS + β3*((1-exp(-t/τ2))/(t/τ2) - exp(-t/τ2))"""
    ...

# 插件注册
MODEL_REGISTRY = {'ns': NelsonSiegel, 'nss': Svensson, ...}
```

验收：RMSE ≤ 2bp（常规市场环境），拟合参数存储为时序，可单独分析（如 β1 时序判断曲线陡峭化趋势）。

##### (5) 关键期限点提取

- 数学方法：二阶导数变号检测拐点
- 业务规则：手工锚定关键期限（如 10Y 是行业关注重点）
- 输出：`KeyTenorPoint(tenor=10.0, type='anchor'/'inflection'/'peak'/'trough', value=2.45)`

### 5.4 L4 分析建模层

#### 5.4.1 走势分析

- 单曲线时序：任意期限点 + 时间范围查询
- 形态演变：多日叠加图 + 三维曲面图（matplotlib plot_surface / ECharts 3D）
- 形态指标：长短期利差、信用利差、流动性利差、斜率、曲率、倒挂识别
- 周期分析：MA / 布林带 / 季节性分解（statsmodels.tsa.seasonal_decompose）

#### 5.4.2 敏感度分析

- **单因子**：平行 / 斜率 / 曲率变化
- **关键利率久期（KRD）**：对每个关键期限点（3M/1Y/3Y/5Y/10Y/30Y）单独冲击 1bp，计算组合 PV 变化
- **PV01 / DV01**：利率变动 1bp 的价值变化

```python
def key_rate_duration(cashflows, curve, key_tenors, shock_bp=1):
    base_pv = present_value(cashflows, curve)
    krd = {}
    for tenor in key_tenors:
        shocked = parallel_shift_at_tenor(curve, tenor, shock_bp/10000)
        krd[tenor] = (present_value(cashflows, shocked) - base_pv) / base_pv / (shock_bp/10000)
    return krd
```

#### 5.4.3 情景模拟

5 类情景：平行 / 陡峭化 / 平坦化 / 历史极端 / 自定义向量

```python
def scenario_shock(curve: Curve, scenario: Scenario) -> Curve:
    if scenario.type == 'parallel':
        return curve + scenario.shock_bp / 10000
    elif scenario.type == 'steepener':
        return curve + scenario.short_shock + (curve.tenor / 30) * (scenario.long_shock - scenario.short_shock)
    elif scenario.type == 'historical':
        historical_curve = load_curve(scenario.historical_date)
        delta = historical_curve - current_curve
        return current_curve + delta
    elif scenario.type == 'custom':
        return curve + scenario.shock_vector  # 逐期限点
```

#### 5.4.4 压力测试

- 多因子组合冲击（利率 + 信用利差 + 流动性利差同时变动）
- 输出：组合价值变化 / NII 变化 / EVE 变化
- 报告导出：满足 IRRBB 报送要求（PDF + Excel）

#### 5.4.5 回溯测试

- 拟合精度回溯：历史时点用模型重构 vs 实际，统计 RMSE 分布
- 预测精度回溯：预测值 vs 实际，计算 MAE、方向准确率
- 策略回测：基于曲线信号（利差均值回归、骑乘策略）的历史表现
- 回溯报告：识别模型失效区间

#### 5.4.6 智能对话（LLM Agent）

见第 7 节。

### 5.5 L5 业务应用层

#### 5.5.1 FTP 定价

- 基础：基于收益率曲线构建 FTP 基准曲线
- 加点：按业务条线（存款、贷款、票据）加点生成定价曲线
- 输出：FTP 价格表、定价基准曲线 API

#### 5.5.2 估值核算

- 输入：债券/衍生品的现金流
- 调用：折现曲线 + 远期利率
- 输出：估值结果 + 估值依据（使用的曲线版本、日期、参数）

#### 5.5.3 ALM 管理

- NII 敏感性：未来 12 个月净利息收入对利率变动的敏感度
- EVE 经济价值：经济价值对利率变动的敏感度
- 利率风险指标：△EVE/EVE、△NII/NII

#### 5.5.4 监管报送

- IRRBB 标准框架下的 6 个监管情景
- 自动生成监管报表模板（可导出）
- 监管口径与平台口径双向映射

---

## 6. 关键业务流程

### 6.1 日常运行流程（T+1 日终）

```
06:00  采集任务触发（中债/外汇/Wind）
       ↓
07:00  数据清洗 + 入库 raw 版本
       ↓
08:00  自动校验 + 派生曲线刷新 + 拟合参数计算
       ↓
08:30  数据质量报告推送（邮件/短信）
       ↓
09:00  业务岗审核 → 调整（如需）→ 发布 official 版本
       ↓
09:30  FTP/估值/ALM 系统调用当日 official 曲线
       ↓ 全天
       盘中增量采集（如启用）
```

### 6.2 曲线构建流程（用户主动发起）

```
选择基础曲线 + 构建方法
       ↓
配置参数（拼接点/拟合模型/插值方法）
       ↓
预览：显示中间结果 + 与原曲线对比图
       ↓
确认执行 → 生成 build_xxx 版本
       ↓
存入 curv_curve_version + 血缘记录
       ↓
可发布为 official 或继续调整
```

### 6.3 敏感度分析流程

```
选择曲线 + 日期 + 资产负债组合（来自 ALMT 或手工上传）
       ↓
选择情景（系统预置 / 历史 / 自定义）
       ↓
执行：逐期限点 PV 重算
       ↓
输出：KRD 向量 + PV01 + NII/EVE 变化
       ↓
导出报告（PDF / Excel / 推送 ALMT）
```

### 6.4 智能问数流程

```
用户输入自然语言问题（如"10年国债近一年走势如何？"）
       ↓
CURVE_CHAT Agent：意图识别 + 实体抽取（曲线=10Y国债、时间=近1年、操作=走势）
       ↓
数据检索：从 curv_rate_data 取数
       ↓
分析计算：统计指标 + 图表生成
       ↓
流式输出（文字 + [chart:line] 标记 + 参考资料面板）
       ↓
可选追问 / 导出 Word 报告
```

---

## 7. 智能体（Agent）编排

### 7.1 Agent 注册表

| Agent | 职责 | 是否 LLM | 输入 | 输出 |
|---|---|---|---|---|
| COLLECT | 报告/数据采集 | 否（脚本） | 数据源配置 | 采集结果 |
| EXTRACT | 曲线数据抽取 | 是 | 原始文档/网页 | 标准化利率点 |
| BUILD | 曲线构建 | 部分（参数推荐） | 曲线定义 + 基础曲线 | 构建后的曲线 + 参数 |
| FIT | 拟合 | 是（模型选择推荐） | 曲线 + 算法库 | 拟合参数 + 残差 |
| ANALYZE | 走势/形态分析 | 否（计算） | 曲线 + 时间范围 | 统计指标 + 图表 |
| SCENARIO | 情景模拟 | 否（计算） | 情景定义 + 资产负债 | 敏感度结果 |
| STRESS | 压力测试 | 否（计算） | 多因子冲击 + 组合 | 报告 |
| BENCHMARK | 利差/对标 | 否 | 多曲线 | 利差指标 |
| ATTRIBUTE | 归因 | 是 | 变化量 | 归因解释 |
| REPORT | 报告生成 | 是 | 分析结果 | Word/PDF |
| CURVE_CHAT | 智能对话入口 | 是 | 自然语言问题 | 流式回答 + 图表 + 参考 |

### 7.2 CURVE_CHAT 主工作流（LangGraph）

```
START
  ↓
[意图识别] ─→ classify intent: data_query | analyze | build | scenario | chat
  ↓
[实体抽取] ─→ extract: curve_code | date_range | tenor | operation
  ↓
[计划生成] ─→ plan steps: [fetch_data, calc_metric, render_chart]
  ↓
[数据获取] → CURVE_DATA_AGENT（直接 SQL/缓存）
  ↓
[计算执行] → ANALYZE / SCENARIO / BUILD 子 Agent
  ↓
[可视化]   → 生成 chart 标记（[chart:line]/[chart:surface]/[chart:heatmap]）
  ↓
[回答组装] → 流式输出文字 + 图表 + 参考资料
  ↓
END
```

### 7.3 智能对话示例

| 用户输入 | Agent 行为 | 输出 |
|---|---|---|
| "10年国债近一年走势" | ANALYZE | [chart:line] + 统计指标 |
| "信用利差最近有什么异常" | BENCHMARK + ATTRIBUTE | 利差图 + 异常归因 |
| "构建一条无风险曲线" | BUILD | 拼接方案推荐 + 预览图 |
| "利率上行100bp，EVE变多少" | SCENARIO | 情景结果表 + KRD 向量 |
| "这条曲线跟昨天比有啥变化" | ANALYZE + ATTRIBUTE | 版本对比 + 归因 |
| "导出今日曲线报告" | REPORT | Word 文档下载 |

---

## 8. 接口设计

### 8.1 内部 REST API

```
# L1 采集
POST   /api/v1/curv/collection/run                    # 手动触发采集
GET    /api/v1/curv/collection/logs                   # 采集日志
GET    /api/v1/curv/collection/monitor                # 采集监控看板

# L2 曲线定义与数据
GET    /api/v1/curv/curves                            # 曲线列表
POST   /api/v1/curv/curves                            # 新建曲线定义
GET    /api/v1/curv/curves/{code}                     # 曲线详情
GET    /api/v1/curv/rates                             # 利率查询 ?curve=10Y&date=2026-08-17
POST   /api/v1/curv/rates/import                      # 批量导入
GET    /api/v1/curv/versions                          # 版本管理
POST   /api/v1/curv/versions/{id}/rollback            # 版本回滚

# L3 构建引擎
POST   /api/v1/curv/build/splice                      # 拼接
POST   /api/v1/curv/build/interpolate                 # 插值
POST   /api/v1/curv/build/smooth                      # 平滑
POST   /api/v1/curv/build/fit                         # 拟合
GET    /api/v1/curv/build/plugins                     # 插件列表

# L4 分析
POST   /api/v1/curv/analyze/trend                     # 走势
POST   /api/v1/curv/analyze/shape                     # 形态指标
POST   /api/v1/curv/analyze/spread                    # 利差
POST   /api/v1/curv/sensitivity/krd                   # 关键利率久期
POST   /api/v1/curv/scenario/run                      # 情景模拟
POST   /api/v1/curv/stress/run                        # 压力测试
POST   /api/v1/curv/backtest/run                      # 回溯测试

# L5 应用
POST   /api/v1/curv/ftp/quote                         # FTP 定价
POST   /api/v1/curv/valuation/bond                    # 债券估值
POST   /api/v1/curv/alm/nii                           # NII 计算
POST   /api/v1/curv/alm/eve                          # EVE 计算
POST   /api/v1/curv/regulatory/irrBB-export          # 监管导出

# 智能体
POST   /api/v1/curv/agent/chat                        # 智能问数（SSE 流式）

# 公共
POST   /api/v1/curv/auth/login                        # 登录
GET    /api/v1/curv/auth/me                           # 当前用户
```

### 8.2 对外标准服务（供 FTP/估值/ALM 系统调用）

```
# 曲线服务（最常用）
GET    /api/v1/curv/service/curve/{code}              # 获取最新曲线
GET    /api/v1/curv/service/curve/{code}?date=YYYY-MM-DD  # 获取历史曲线
POST   /api/v1/curv/service/discount-factor           # 折现因子查询
POST   /api/v1/curv/service/forward-rate              # 远期利率查询
POST   /api/v1/curv/service/interpolate               # 自定义期限点插值

# 鉴权：API Key + IP 白名单
# 限流：100 QPS / Key
# 监控：调用日志 + 延迟监控
```

### 8.3 SSE 流式对话协议

```
POST /api/v1/curv/agent/chat
Content-Type: application/json
{ "session_id": "xxx", "query": "10年国债走势如何" }

→ SSE 流式响应
data: {"type":"chunk","content":"10年期国债收益率"}
data: {"type":"chunk","content":"近一年从 2.45% 上升至 2.68%"}
data: {"type":"chart","chart_type":"line","data_url":"/tmp/chart_xxx.png"}
data: {"type":"references","items":[{"title":"..."}]}
data: {"type":"done"}
```

---

## 9. 前端产品设计

### 9.1 主导航

```
CURV 平台
├─ 数据中心
│   ├─ 数据源配置
│   ├─ 采集监控
│   └─ 数据质量看板
├─ 曲线管理
│   ├─ 曲线定义
│   ├─ 利率数据查询
│   ├─ 版本管理
│   └─ 派生曲线配置
├─ 曲线构建
│   ├─ 拼接
│   ├─ 插值
│   ├─ 平滑
│   ├─ 拟合（NS/NSS/...）
│   └─ 关键期限点
├─ 走势分析
│   ├─ 时序分析
│   ├─ 形态演变
│   ├─ 形态指标
│   └─ 周期分析
├─ 风险计量
│   ├─ 关键利率久期
│   ├─ 情景模拟
│   ├─ 压力测试
│   └─ 回溯测试
├─ 业务应用
│   ├─ FTP 定价
│   ├─ 估值核算
│   ├─ ALM 工具
│   └─ 监管报送
└─ 智能问数（对话式入口）
```

### 9.2 关键页面原型

**1. 曲线构建工作台**
```
┌─────────────────────────────────────────────────────────────┐
│ 曲线构建工作台  [10Y国债 2026-08-17]                       │
├──────────────────┬──────────────────────────────────────────┤
│ ① 基础曲线       │  ④ 预览                                  │
│ ○ 国债收益率     │  ┌────────────────────────────────────┐ │
│ ○ 政策性金融债   │  │   曲线对比图（原始 vs 构建后）       │ │
│ ○ 同业存单       │  │   [ECharts Line + 双 Y 轴]          │ │
│                  │  └────────────────────────────────────┘ │
│ ② 构建方法       │                                          │
│ ○ 拼接 ○ 拟合    │  ⑤ 操作日志                              │
│ ○ 插值 ○ 平滑    │  • 2026-08-17 10:23:45 NS拟合 β0=2.45  │
│                  │  • 2026-08-17 10:24:12 参数调优            │
│ ③ 参数           │                                          │
│ 拼接点: [1Y]    │  [保存为版本]  [发布official]              │
│ 模型:   [NS]    │                                          │
└──────────────────┴──────────────────────────────────────────┘
```

**2. 智能问数界面**
- 类 ChatGPT 风格，左侧历史会话
- 消息体支持 Markdown + 内嵌图表 + 参考资料侧栏
- 一键导出当前会话为 Word

**3. 走势分析大屏**
- 顶部：当前曲线快照（实时刷新）
- 左中：多期限叠加图（1Y/5Y/10Y/30Y）
- 右中：形态指标仪表盘（利差/斜率/曲率/倒挂）
- 底部：异常事件时间轴

---

## 10. 非功能设计

### 10.1 性能指标

| 指标 | 目标 | 验收 |
|---|---|---|
| 曲线查询响应 | < 500ms | 压测报告 |
| 单曲线拟合 | < 1s | 30 期限点 NS 拟合 |
| 万级资产组合敏感度 | < 30s | 10000 笔现金流 KRD |
| 大盘渲染 | < 3s | 含 3D 曲面图 |
| 智能对话首字延迟 | < 2s | SSE 首 chunk |

### 10.2 可用性

- 系统可用性 ≥ 99.9%（年停机 ≤ 8.76 小时）
- 集群部署 + 故障自动切换
- 关键操作事务化（构建→入库→血缘）

### 10.3 可追溯性

- 所有曲线数据 + 构建参数 + 分析结果全链路留痕
- 任何输出可一键回溯至原始数据 + 操作参数 + 操作人
- 审计日志保留 ≥ 5 年

### 10.4 可扩展性

- 算法插件化：新增拟合/平滑/插值模型无需改核心代码
- 数据源插件化：新增数据源 = 新增 Adapter 类
- 前端组件化：曲线图/曲面图/热力图可复用

### 10.5 安全性

- 三级权限：按曲线 × 机构 × 操作（读/编辑/发布/管理）
- 字段级脱敏（敏感数据如水印）
- API Key + IP 白名单 + 限流
- 操作审计全留痕

### 10.6 部署架构

| 维度 | 设计 |
|---|---|
| 服务路径 | `/opt/curv/` |
| systemd 服务 | `curv-backend`（端口 8003） |
| 前端 | `/var/www/curv/`，vite `base:'/curv/'` |
| nginx | `/curv/{assets, /, api}` 三个 location |
| 数据库 | `curv_db`，用户 almd/Almd@2026 |
| 缓存 | Redis 缓存常用曲线查询（TTL 300s） |
| 监控 | Prometheus + Grafana（曲线采集成功率、查询延迟） |

---

## 11. 实施路径（沿用需求 6，但更具体）

### 一期：数据底座（1-3 月）

**目标**：建成统一曲线数据底座，替代手工 Excel 管理

**交付物**：
- ✅ 数据源适配器框架（中债、Shirobor、Wind、手工）
- ✅ 采集调度 + 监控看板
- ✅ 数据清洗流水线
- ✅ 曲线定义配置 + 利率数据存储
- ✅ 版本管理 + 血缘（基础版）
- ✅ 基础插值（线性、对数线性、PCHIP）
- ✅ 利率查询 API + 可视化页面

**上线指标**：
- 接入 ≥ 5 个数据源（中债国债/政金/中票、外汇 Shibor/Repo）
- 采集成功率 ≥ 95%
- 替代 ≥ 80% 手工 Excel 维护工作

### 二期：构建与分析（3-6 月）

**目标**：具备完整曲线构建和分析能力

**交付物**：
- ✅ 曲线拼接 + 平滑（多算法）
- ✅ Nelson-Siegel / Svensson 拟合
- ✅ 关键期限点提取
- ✅ 走势分析 + 形态指标 + 利差分析
- ✅ 关键利率久期（KRD）
- ✅ 情景模拟（5 类预置情景）
- ✅ 压力测试 + IRRBB 报表导出
- ✅ 智能问数基础版（数据查询 + 简单分析）

**上线指标**：
- 拟合 RMSE ≤ 2bp 达标率 ≥ 90%
- KRD 计算支持万级现金流组合
- 智能问数覆盖率 ≥ 60% 业务问题

### 三期：预测与闭环（6-12 月）

**目标**：形成曲线全生命周期管理闭环

**交付物**：
- ✅ 利率预测模型（ARIMA / 宏观回归 / LSTM/XGBoost）
- ✅ 回溯测试 + 模型失效识别
- ✅ 与 ALMT/FTP/估值系统深度集成
- ✅ 监管报表自动生成（IRRBB 标准）
- ✅ 智能定价建议（基于预测曲线 + 历史回测）
- ✅ 多币种支持（二期评估，一期仅人民币）
- ✅ 完整智能体闭环（CHAT → 业务执行 → 反馈）

**上线指标**：
- 预测方向准确率 ≥ 65%
- FTP/估值/ALM 系统 100% 通过 API 调用曲线
- 监管报表自动化率 ≥ 80%

---

## 12. 风险与依赖

### 12.1 外部依赖

| 依赖项 | 风险 | 缓解 |
|---|---|---|
| 中债/外汇数据源授权 | 商务谈判周期长 | 一期先做手工 + 文件导入，接口并行申请 |
| 行内 SSO | 集成需 IT 配合 | 提供本地账号 fallback |
| 资产负债组合数据 | 涉及敏感数据 | 与 ALMT 共享数据通路 |
| DeepSeek LLM | 服务稳定性 | 多 LLM fallback（qwen/gpt-4o/mock） |

### 12.2 技术风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| 拟合精度不达标（>2bp） | 业务不可用 | 多算法并行 + 人工调参 + 业务确认阈值 |
| 时序数据量爆炸 | 性能问题 | 分区 + 压缩 + 冷热数据分层 |
| 多源数据口径差异 | 数据一致性 | 统一标准化层 + 口径说明文档 |
| 智能对话幻觉 | 误导业务 | 强制引用数据源 + 置信度标识 |

### 12.3 开放问题（待与业务方确认）

1. 拟合精度 2bp 阈值是否覆盖所有曲线？（可能政金债波动大）
2. 敏感度所需资产负债组合数据来源？→ 建议对接 ALMT
3. 三期 ML 模型纳入与否？→ 取决于数据积累
4. 多币种优先级？→ 一期仅人民币
5. 是否需要支持交易类实时曲线？→ 一期仅日终，盘中增量二期

---

## 13. 与现有平台的集成

### 13.1 共用基础设施

| 基础设施 | CURV 复用方式 |
|---|---|
| MySQL 用户 | almd/Almd@2026（与 ALMD/IALMD/ALMT 一致），新建库 `curv_db` |
| LLM 配置 | `sys_llm_config` 表（多 LLM 路由） |
| 用户/权限 | `sys_user` / `sys_role` / `sys_permission`（统一 SSO） |
| 审计日志 | `sys_audit_log`（跨系统统一） |
| 部署 | 端口 8003，路径 /opt/curv/，前端 /var/www/curv/ |
| 导航门户 | 统一门户增加 /curv/ 入口 |

### 13.2 与 ALMD / IALMD / ALMT 的数据交互

| 方向 | 内容 | 方式 |
|---|---|---|
| CURV → ALMD | 提供收益率曲线供 IRRBB 计量 | REST API |
| CURV → ALMT | 提供基准曲线供计划模拟 | REST API + 文件导出 |
| CURV → IALMD | 提供折现曲线供保险估值 | REST API（多币种） |
| ALMT → CURV | 提供资产负债组合数据 | REST API / 数据库视图 |
| 统一门户 | CURV 作为独立子路径 | nginx location |

### 13.3 命名与规范

- 表前缀：`curv_`
- API 前缀：`/api/v1/curv/`
- 前端基础路径：`/curv/`
- 服务端口：8003（避开 ALMD 8000、IALMD 8002、ALMT 8001）
- 包名：`services.curv.*`、`agents.curv.*`

---

## 14. 总结

CURV 平台通过"**数据底座 + 构建引擎 + 分析建模 + 业务应用 + 智能对话**"五层架构，配合可配置、可追溯、可扩展的设计原则，实现收益率曲线的全生命周期管理。

核心差异化：
1. **可配置可追溯** —— 所有构建操作可复现，满足监管审计
2. **插件化扩展** —— 新数据源、新算法 0 改动核心接入
3. **业务闭环** —— 不只是分析，更是定价基准、风险计量、监管报送的统一入口
4. **智能对话** —— 自然语言问数，降低使用门槛

下一步建议：
- 优先确认 12.3 中的开放问题（特别是拟合精度阈值和资产负债数据来源）
- 一期优先接入中债国债 + 政金债 + Shibor 三条核心曲线
- 与 ALMT 团队对齐数据通路设计