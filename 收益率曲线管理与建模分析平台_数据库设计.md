# 收益率曲线管理与建模分析平台 (CURV) — 数据库设计

> 版本：V1.0   日期：2026-08-17
> 配套脚本：[curv_db.sql](./curv_db.sql)

## 0. 设计原则

| 原则 | 说明 |
|---|---|
| 与 ALMD/IALMD/ALMT 风格一致 | 表前缀 `curv_`/`sys_`、公共字段（id/status/is_deleted/created_by/updated_by/created_at/updated_at）、utf8mb4 |
| 三维模型 | `curv_rate_data` 以「曲线×日期×期限点」为主键，支撑任意切片查询 |
| 版本血缘 | 每条利率保留 `source_version`（raw/cleaned/official/build_xxx/derived），`curv_lineage` 记录完整链路 |
| 字典驱动 | 期限点/利率类型/计息基准/复利方式/插件模型全部字典化，便于扩展 |
| 插件化扩展 | `curv_plugin_model` 注册算法插件（拟合/插值/平滑），新增模型 = 1 行注册 |
| 冷热分层 | 一期 MySQL 单表 + 复合索引；二期/三期升级 TimescaleDB hypertable 按月分区 |

## 1. 数据库信息

| 项 | 值 |
|---|---|
| 数据库名 | `curv_db` |
| 字符集 | utf8mb4 |
| 排序规则 | utf8mb4_unicode_ci |
| 数据库用户 | `almd`（与 ALMD/IALMD/ALMT 共用） |
| 密码 | `Almd@2026`（URL 中 `@` 需编码为 `%40`） |
| 连接示例 | `mysql+pymysql://almd:Almd%402026@localhost:3306/curv_db?charset=utf8mb4` |

## 2. 表结构总览（21 张表）

```
基础字典 (4)
  curv_tenor_standard       标准期限点（1D/7D/1M/.../30Y）
  curv_rate_type_dict       利率类型（即期/远期/YTM/平价/零息）
  curv_day_count_dict       计息基准（ACT/365、ACT/360、30/360、ACT/ACT）
  curv_compound_dict        复利方式（单利/复利/连续复利）
  curv_plugin_model         拟合/插值/平滑算法插件

L1 数据采集层 (4)
  curv_data_source          数据源配置
  curv_collection_task      采集任务定义
  curv_collection_log       采集执行日志
  curv_collection_template  文件导入模板

L2 数据管理层 (6)
  curv_curve_definition     曲线定义
  curv_rate_data            利率数据（核心事实表）
  curv_curve_version        曲线版本（raw/cleaned/official）
  curv_derived_curve        派生曲线定义
  curv_lineage              血缘追踪
  curv_validation_rule      校验规则

L3 曲线构建层 (2)
  curv_fitting_param        拟合参数（时序存储）
  curv_key_tenor            关键期限点

L4 分析建模层 (5)
  curv_shape_metric         形态指标
  curv_scenario             情景定义
  curv_scenario_result      情景结果
  curv_backtest_result      回溯测试结果
  curv_smart_dialogue       智能对话历史

L5 业务应用层 (2)
  curv_ftp_spread_rule      FTP 加点规则
  curv_regulatory_report    监管报送记录

系统表 sys_ (4，沿用 ALMD 模式)
  sys_user / sys_role / sys_user_role / sys_llm_config / sys_audit_log
```

## 3. 核心表设计详解

### 3.1 `curv_rate_data`（事实表，平台核心）

**业务定位**：所有分析、构建、查询的事实基础。

**主键策略**：
- 业务主键：(curve_code, trade_date, tenor, source_version) —— 唯一约束
- 物理主键：id (bigint auto_increment)

**索引设计**：
```sql
UNIQUE  (curve_code, trade_date, tenor, source_version)  -- 防重
INDEX   (curve_code, trade_date)                         -- 单曲线时序
INDEX   (trade_date)                                      -- 全市场快照
INDEX   (curve_code, tenor, trade_date)                  -- 单期限历史
INDEX   (source_version)                                 -- 版本检索
INDEX   (data_status)                                     -- 状态过滤
```

**字段说明**：

| 字段 | 类型 | 说明 |
|---|---|---|
| `curve_code` | varchar(64) | 关联 curv_curve_definition.code |
| `trade_date` | date | 数据日期 |
| `tenor` | varchar(16) | 期限编码（关联 curv_tenor_standard.tenor_code） |
| `rate_value` | decimal(12,6) | **百分数原值**（如 2.45 表示 2.45%） |
| `source_version` | varchar(32) | raw/cleaned/official/build_xxx/derived |
| `data_status` | varchar(16) | active/revised/deleted/pending_review |
| `is_adjusted` | tinyint | 是否人工调整 |
| `adjust_reason` | varchar(500) | 调整原因（必填） |
| `adjusted_by/at` | varchar/datetime | 调整人/时间 |

**典型查询**：

```sql
-- 1. 单曲线当日时序
SELECT tenor, rate_value FROM curv_rate_data
WHERE curve_code='cnb_treasury_yield'
  AND trade_date='2026-08-17'
  AND source_version='official'
ORDER BY FIELD(tenor,'1M','3M','6M','1Y','5Y','10Y','30Y');

-- 2. 单期限近一年走势
SELECT trade_date, rate_value FROM curv_rate_data
WHERE curve_code='cnb_treasury_yield'
  AND tenor='10Y'
  AND source_version='official'
  AND trade_date BETWEEN '2025-08-17' AND '2026-08-17'
ORDER BY trade_date;

-- 3. 多期限叠加
SELECT trade_date,
       MAX(CASE WHEN tenor='1Y' THEN rate_value END) AS rate_1y,
       MAX(CASE WHEN tenor='5Y' THEN rate_value END) AS rate_5y,
       MAX(CASE WHEN tenor='10Y' THEN rate_value END) AS rate_10y
FROM curv_rate_data
WHERE curve_code='cnb_treasury_yield'
  AND source_version='official'
  AND trade_date >= '2025-08-17'
GROUP BY trade_date;

-- 4. 全曲线当日快照
SELECT curve_code, tenor, rate_value FROM curv_rate_data
WHERE trade_date='2026-08-17'
  AND source_version='official'
ORDER BY curve_code, FIELD(tenor,'1M','3M','6M','1Y','5Y','10Y','30Y');
```

**存储规模估算**：
- 一期：13 曲线 × 14 期限 × 1 天 = ~200 行/天 → 5 年 ~36 万行
- 二期扩展至 30 曲线 → 5 年 ~80 万行，MySQL 完全胜任
- 三期扩展至多币种 + 时序高频 → 升级 TimescaleDB hypertable，按月分区

### 3.2 `curv_curve_definition`（曲线定义）

**业务定位**：曲线的元数据与采集映射，配置一次长期复用。

**关键设计**：
- `curve_type` 区分 `base`（基础）/ `derived`（派生）/ `manual`（手工）
- `tenor_set_json` 存储期限点列表，适配非标准期限
- `source_mapping_json` 存储数据源字段映射，支持多数据源

### 3.3 `curv_curve_version`（版本管理）

**业务定位**：同一曲线同一日期支持多版本并存：raw → cleaned → official。

**版本状态机**：
```
raw（采集入库）
  ↓ 自动清洗
cleaned（清洗后）
  ↓ 派生计算
derived（派生曲线）
  ↓ 拟合计算
build_xxx（构建结果）
  ↓ 人工调整
adjusted（人工调整）
  ↓ 业务确认
official（正式发布）
  ↓ 历史归档
archived
```

### 3.4 `curv_lineage`（血缘追踪）

**业务定位**：每条利率值可向上回溯全链路。

**数据结构**：
```json
{
  "data_id": 12345,
  "curve_code": "cnb_treasury_yield",
  "trade_date": "2026-08-17",
  "tenor": "10Y",
  "source_version": "official",
  "upstream_sources": [
    {"step": "采集", "source": "china_bond_treasury", "time": "2026-08-17 06:00:01", "operator": "collector-bot"},
    {"step": "清洗", "rule": "字段映射 + ACT/365 标准化", "time": "2026-08-17 07:15:23"},
    {"step": "拟合", "model": "nelson_siegel", "params": "β0=2.78 β1=-0.85 β2=-1.20", "time": "2026-08-17 08:10:05"},
    {"step": "人工调整", "delta": "+0.5bp", "reason": "与外汇交易中心报价对账差异", "operator": "张三", "time": "2026-08-17 09:32:18"},
    {"step": "业务发布", "operator": "李四", "time": "2026-08-17 09:00:00"}
  ],
  "operations": [
    {"step": "清洗", "rule": "缺失值填充=N/A", "time": "2026-08-17 07:15:23"},
    ...
  ],
  "reason": "..."
}
```

### 3.5 `curv_fitting_param`（拟合参数时序）

**业务定位**：拟合参数（如 NS 的 β0/β1/β2）作为时序数据存储，可单独查询和分析。

**典型查询**：
```sql
-- 10Y 国债曲线斜率 β1 时序（判断陡峭/平坦化）
SELECT trade_date, JSON_EXTRACT(params_json,'$.beta1') AS beta1
FROM curv_fitting_param
WHERE curve_code='cnb_treasury_yield'
  AND version_no='official'
  AND model_code='nelson_siegel'
  AND trade_date >= '2025-01-01'
ORDER BY trade_date;
```

### 3.6 `curv_scenario_result`（情景结果）

**业务定位**：记录情景模拟/压力测试的完整结果，支撑 IRRBB 监管报送。

**字段亮点**：
- `pv_change / nii_change / eve_change`：三个核心风险指标
- `krd_vector_json`：关键利率久期向量（{3M:0.05, 1Y:0.18, ..., 10Y:3.85}）
- `scenario_run_code`：同一批次运行的所有结果可用同一 run_code 关联

### 3.7 `curv_smart_dialogue`（智能对话历史）

**业务定位**：CURVE_CHAT Agent 的对话持久化，支持会话回看与审计。

**关键字段**：
- `agent_trace_json`：Agent 编排轨迹（哪个 Agent 何时执行、用了什么数据）
- `result_json`：结构化结果（图表数据/参考资料）
- `tokens_in/out` + `duration_ms`：LLM 调用成本与耗时监控

### 3.8 `curv_plugin_model`（算法插件注册表）

**业务定位**：新增拟合/插值/平滑算法 = 新增 1 行注册，无需改核心代码。

**示例**：
```sql
-- 注册一个自定义拟合模型
INSERT INTO curv_plugin_model (code, name, type, impl_path, params_schema, description)
VALUES ('my_custom_fit', '行内自研模型', 'fit', 'curv.engines.fitting:MyCustomFit', '{"alpha": {"type": "float"}}', '行内自研');
```

## 4. 与 ALMD/IALMD/ALMT 的命名规范一致性

| 维度 | 规范 | CURV 遵循 |
|---|---|---|
| 表前缀 | `curv_`（业务） / `sys_`（系统） | ✅ |
| 公共字段 | id/status/is_deleted/created_by/updated_by/created_at/updated_at | ✅ |
| JSON 字段后缀 | `_json` | ✅ |
| 字符集 | utf8mb4 | ✅ |
| 主键策略 | bigint auto_increment | ✅ |
| 字典表 | 单独维护（如 curv_tenor_standard） | ✅ |
| 用户/角色/审计 | 沿用 sys_user/sys_role/sys_audit_log | ✅ |
| LLM 配置 | 存数据库 sys_llm_config，非 .env | ✅ |
| 数据源表 | curv_data_source（业务） + sys_user（系统用户） | ✅ |
| URL 编码 | 密码中 `@` → `%40` | ✅ |

## 5. 执行步骤

```bash
# 1. 上传 SQL 到服务器（43.143.253.186）
scp curv_db.sql almd@43.143.253.186:/tmp/

# 2. SSH 到服务器
ssh almd@43.143.253.186

# 3. 执行建库脚本
mysql -u almd -p'密码' < /tmp/curv_db.sql

# 4. 验证
mysql -u almd -p'密码' curv_db -e "SHOW TABLES; SELECT COUNT(*) FROM curv_rate_data;"
# 预期：21 张表，curv_rate_data 0 行（待采集数据）
```

**注意**：MySQL `local_infile=OFF` 时无法用 `LOAD DATA LOCAL INFILE`，数据导入参考 ALMT 的 `import_server.py`（pymysql executemany）。

## 6. 性能与扩展

### 6.1 一期性能目标（MySQL 单库）

| 指标 | 目标 | 说明 |
|---|---|---|
| 利率查询响应 | < 500ms | 索引覆盖 |
| 曲线时序查询 | < 1s | 250 个交易日 |
| 多曲线快照 | < 1s | 全曲线一次性读取 |
| 拟合参数查询 | < 500ms | JSON_EXTRACT 索引 |

### 6.2 二期/三期扩展策略

- **数据量超 100 万** → 升级 TimescaleDB hypertable，按月分区
- **采集频率提升**（盘中 30min）→ 增加 Redis 缓存常用曲线
- **多币种** → 增加 `currency` 索引 + 按币种分表（可选）
- **历史归档** → 5 年以上数据归档到历史库，主库只保留热数据

## 7. 安全与审计

- 密码字段（`sys_user.password_hash`）用 bcrypt 存储
- 所有操作写 `sys_audit_log`（含 IP、UA、参数、耗时）
- 审计日志保留 ≥ 5 年
- 行级权限：按曲线 × 机构 × 角色三级（应用层控制）

## 8. 下一步建议

1. **执行 SQL 建库** → 验证 21 张表全部创建成功
2. **插入测试数据** → 手工录入 5-10 个期限点的 2026-08-17 数据
3. **配置数据源** → 在 `curv_data_source` 中新增实际的中债 API 配置
4. **对接 LLM** → 在 `sys_llm_config` 中填入真实 API Key
5. **启动后端服务** → 初始化 FastAPI 项目，连接 curv_db